services:
  - type: web
    name: ps-business-suites
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python render_bootstrap.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    healthCheckPath: /
